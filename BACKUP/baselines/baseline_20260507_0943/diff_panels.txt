--- NewOrigami.Panels.html	2026-05-03 11:11:39
+++ backup_broken_session/NewOrigami.Panels.html	2026-05-03 10:58:08
@@ -5,7 +5,7 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Origami Adventure Panels - Tactical Symmetry Pro</title>
     <link rel="icon" href="data:,">
-    <script src="https://unpkg.com/lucide@latest"></script>
+    <script src="js/lucide.min.js"></script>
     <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@700;800;900&display=swap" rel="stylesheet">
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
     <script src="js/data/cards.js"></script>
@@ -280,7 +280,7 @@
         .roll-btn i { margin-right: 10px; font-size: 20px; }
 
         .card-column { position: relative; width: var(--card-w); height: var(--card-h); perspective: 1000px; cursor: pointer; flex: 0 0 var(--card-w); transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1); margin-right: 0px; }
-        .card-column.focused { z-index: 30; transform: translateY(-10px); }
+        .card-column.focused { z-index: 30; }
         .card-column.focused .guide-card[data-depth="0"] { box-shadow: 0 15px 30px var(--nm-shadow-dark), 0 0 20px var(--theme-clr); border-color: var(--theme-clr); }
 
         @media (max-width: 1100px) {
@@ -1032,7 +1032,7 @@
                 width: 70%;
                 height: 45%;
                 border-radius: 50%;
-                background: linear-gradient(180deg, rgba(255,255,255,0.25) 0%, rgba(255,255,255,0.05) 50%, transparent 100%);
+                background: linear-gradient(180deg, rgba(255,255,255,0.08) 0%, rgba(255,255,255,0.02) 50%, transparent 100%);
                 filter: blur(1px);
                 z-index: 11;
                 pointer-events: none;
@@ -1046,7 +1046,7 @@
                 width: 80%;
                 height: 25%;
                 border-radius: 50%;
-                background: radial-gradient(ellipse at center, rgba(255,255,255,0.12) 0%, transparent 70%);
+                background: radial-gradient(ellipse at center, rgba(255,255,255,0.04) 0%, transparent 70%);
                 filter: blur(4px);
                 z-index: 11;
                 pointer-events: none;
@@ -2342,10 +2342,10 @@
             loadDeck(categories);
             UXAI.initViewportManager();
             
-            document.getElementById('bottom-panel').onwheel = e => {
+            document.getElementById('bottom-panel').addEventListener('wheel', e => {
                 if (document.activeElement.id !== 'bottom-panel' || wheelThrottle) return;
                 e.preventDefault(); wheelThrottle = true; cycle(focusedColIndex, e.deltaY > 0); setTimeout(() => wheelThrottle = false, 150);
-            };
+            }, { passive: false });
             const params = new URLSearchParams(window.location.search);
             const mode = params.get('mode');
             if (mode !== 'combat') {
@@ -2565,17 +2565,29 @@
             if (a === 'ATTACK') {
                 playAvatarAnim('slash');
                 
-                let hasUsableMissile = false;
+                let firstMissileCat = null;
+                let firstMissileCard = null;
                 combatCategories.forEach(cat => {
-                    if (cat.id === 'MISSILE' && cat.cards && cat.cards.length > 0) {
+                    if (!firstMissileCat && cat.id === 'MISSILE' && cat.cards && cat.cards.length > 0) {
                         if (cat.qty === undefined || cat.qty > 0) {
-                            hasUsableMissile = true;
+                            firstMissileCat = cat;
+                            firstMissileCard = cat.cards[0];
                         }
                     }
                 });
                 
-                if (window.currentMonsterDistanceFeet > 5 && !hasUsableMissile) {
-                    window.parent.postMessage({ type: 'LOG_EVENT', logType: 'system', text: 'No Missile Playing Cards Found' }, '*');
+                if (window.currentMonsterDistanceFeet > 5) {
+                    if (firstMissileCard) {
+                        // Launch the missile automatically!
+                        window.parent.postMessage({ type: 'FPV_ACTION', action: firstMissileCard }, '*');
+                        
+                        // Deduct qty internally so it reflects when loadDeck happens
+                        if (firstMissileCat.qty !== undefined && firstMissileCat.qty > 0) {
+                            firstMissileCat.qty--;
+                        }
+                    } else {
+                        window.parent.postMessage({ type: 'LOG_EVENT', logType: 'system', text: 'No Missile Playing Cards Found' }, '*');
+                    }
                 }
                 
                 loadDeck(combatCategories);
@@ -3013,7 +3025,6 @@
                 const ez = document.getElementById('encounter-zone');
                 if (!ez.classList.contains('active')) {
                     window.logEvent('You encounter a Yakuza Goblin!', 'system');
-                    window.logEvent('DMG: (LVL + WPN + SPC)', 'system');
                 }
                 ez.classList.add('active');
                 document.getElementById('event-log-container').classList.add('active');
@@ -3348,7 +3359,7 @@
                 if (window.showLcdEvent) window.showLcdEvent(e.data.text || "SYS_ERR");
                 window.logEvent(e.data.text || "SYS_ERR", 'system');
             } else if (e.data && e.data.type === 'LOG_EVENT') {
-                window.logEvent(e.data.text, e.data.logType || 'system');
+                window.logEvent(e.data.text || e.data.message, e.data.logType || 'system');
             } else if (e.data && e.data.type === 'PLAYER_MOVE') {
                 if (e.data.rot !== undefined) updateCompass(e.data.rot);
             } else if (e.data && e.data.type === 'PLAYER_ROT_REALTIME') {
@@ -3399,6 +3410,9 @@
                     document.body.classList.add('is-moving');
                     hideExitModal(); // Hide if player walks away
                     
+                    // Moving auto-hides the log after 3s if no new events
+                    resetLogHideTimer(); 
+
                     const walk = avatarActions ? avatarActions.walk : null;
                     if (walk && avatarCurrentAction !== walk) {
                         walk.setEffectiveTimeScale(1.0);
@@ -3409,6 +3423,9 @@
                 }
                 else {
                     document.body.classList.remove('is-moving');
+                    // Entering idle starts the hide timer
+                    resetLogHideTimer();
+
                     if (avatarIdleAction && avatarCurrentAction !== avatarIdleAction) {
                         const isWalkCycle = (avatarActions && avatarIdleAction === avatarActions.walk);
                         avatarIdleAction.setEffectiveTimeScale(isWalkCycle ? 0.05 : 1.0);
@@ -3442,14 +3459,30 @@
         }
         window.addEventListener('resize', updateViewportReadout);
         
+        let logHideTimeout = null;
+        function resetLogHideTimer() {
+            if (logHideTimeout) clearTimeout(logHideTimeout);
+            logHideTimeout = setTimeout(() => {
+                // Do not hide if combat is active or user is focused on chat
+                const ez = document.getElementById('encounter-zone');
+                const chatInput = document.querySelector('.chat-input');
+                if (ez && ez.classList.contains('active')) return;
+                if (chatInput && document.activeElement === chatInput) return;
+                
+                document.getElementById('event-log-container').classList.remove('active');
+            }, 3000);
+        }
+
         // Log System & Chat Input bindings
         window.logEvent = function(text, type = 'system') {
             const container = document.getElementById('event-log-container');
             if (!container) return;
             
-            // Auto open panel for any system or combat dialogue so the user can actually see it!
-            if (type === 'chat' || type === 'system' || type === 'damage') {
+            // Auto open panel for any system or combat dialogue
+            if (type === 'chat' || type === 'system' || type === 'damage' || type === 'room') {
                 container.classList.add('active');
+                // Any new event prevents auto-hide for another 3s
+                resetLogHideTimer();
             }
             
             const entry = document.createElement('div');

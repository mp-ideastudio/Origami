#!/usr/bin/env node
/**
 * G-AUDIO — Stub. Real implementation lands with T2.1 (AudioSystem).
 *
 * Until then, exits 0 so dependent tasks don't block.
 */
console.log('[smoke:audio] stub — no audio assets yet (real check lands in T2.1)');
process.exit(0);

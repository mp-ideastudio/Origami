#!/usr/bin/env node
/**
 * G-SAVE — Stub. Real implementation lands with T0.3 (SaveSystem).
 *
 * Will load NewOrigami.8.html, write a known state via the save API,
 * reload, and assert all 9 fields round-trip. Until T0.3 there is no
 * save API to test.
 */
console.log('[smoke:save] stub — no SaveSystem yet (real check lands in T0.3)');
process.exit(0);
